import { styled } from '@mui/material/styles'

export const AboutUsMui = styled('section')(({ theme }) => ({
  color: theme.palette.text.primary,
  backgroundColor: theme.palette.background.default,
}))
